/*const mongoose=require("mongoose")
const cartSchema=new mongoose.Schema({
    
    user: {
        type: mongoose.Schema.Types.ObjectId,
  
        ref: 'User',
      },
  cartDetails:[{ref:'orderDetailSchema'
  },
],
      
});

module.exports=Cart=mongoose.model("cart",cartSchema)*/